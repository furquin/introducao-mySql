SELECT product_name, id FROM products ORDER BY product_name;

-- SELECT => seleciona algo
-- product_name, id => coluna que será seleciona
-- FROM => de onde será selecionado
-- products => tabela que será usada para buscar as colunas
-- ORDER BY => ordena itens a partir de coluna 
-- product_name => coluna que será usada para ordenar
